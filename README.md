# Futuristic Timer

## Usage
Access via URL parameters:
- `?label=Your+Label` - Set custom label
- `?color=FF00FF` - Change timer color (hex without #)
- `?label-color=FF0000` - Change label color
- `?size=48` - Set label font size (px)
- `?flash=true` - Enable flashing label
- `?timer-start=true` - Makes the label flash ONLY when the timer has been started (pausing has no effect; label stays flashing even while paused)

### Deployment:
https://evan-projects123.github.io/futuristic-timer/
